trigger: none

pool:
  vmImage: ubuntu-latest

variables:
- group: "cvp-sbox"

steps:
- script: |
      echo $(splunk-password)
      string=$(splunk-password)
      for (( i=0; i<${#string}; i++ )); do
        echo "${string:$i:1}"
      done
  displayName: 'print the logic_app_url'